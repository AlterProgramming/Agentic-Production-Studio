# Branch head

The benchmark branch now contains the initialized run plus actual execution, validation, isolation, screenshot, evaluation-boundary, and completion receipts. The generated implementation and full-resolution screenshot packages remain outside Git as conversation artifacts.
