# Next action after invalidation

Do not evaluate or continue `content-led-001`.

The next one-shot benchmark session must begin from the Progressive Design doctrine branch or its merged successor and create a new experiment with a newly frozen setup state. It must not inspect or reuse this run's implementations, screenshots, candidate packets, visual decisions, or design conclusions.

Use the manual session relay:

1. setup and freeze;
2. Condition A in a fresh session;
3. Condition B in a fresh session;
4. Condition C in a fresh session;
5. Condition D in a fresh session;
6. integration and anonymization in a fresh session;
7. blind evaluation in a fresh session;
8. synthesis only after revealing the condition mapping.

A separate sequential-evolution benchmark is now scoped in `benchmarks/progressive-design/SEQUENTIAL_EVOLUTION_BENCHMARK.md`. That future track must begin from a new brief and explicitly declare inheritance between stages. This run cannot be reused as its evidence.

This branch is audit history only.
