# Sealed launch packet — Progressive Design with Spatial Intelligence

Create the strongest complete public product supported by the frozen material. Apply integrity, composition, system, identity, interaction, authorship, and critique/elevation as defined below, then use Spatial Intelligence and a private construction layer while originating the product.

Progressive Design requirements: preserve substance; resolve composition before ornament; create a coherent but non-templated system; derive identity from the material; use interaction structurally; introduce a small number of authored decisions; and critique every page against the product's strongest moments.

Spatial Intelligence requirements:
- use an explicit scaffold privately to establish alignment, scale, spacing, proportion, focal axes, and responsive boundaries;
- advance toward latent structure that remains legible when visible guides are removed;
- derive spatial behavior from chronology, relationships, comparison, exploration, or documentary sequence only where it improves understanding;
- preserve intent across viewport and interaction state rather than merely collapsing desktop;
- permit deliberate deviation only after the spatial system is coherent;
- public geometry must improve orientation, comparison, sequence, causality, scale, or spatial understanding.

Private Construction Layer:
- construction grids, axes, crop guides, density maps, motion paths, breakpoint diagnostics, hierarchy maps, component classifications, and verification notes may exist only as authoring aids;
- debug geometry must default off and must not be required for the public composition to work;
- anything created primarily to help construction, verification, or explanation remains internal.

Do not apply a separate Visitor-Facing Reconciliation pass.

## Execution contract

Experiment: `content-led-001`
Task family: `content-led`
Frozen brief: `../../FROZEN_BRIEF.md`
Shared content: `../../fixtures/content/archive.json`
Shared images: `../../fixtures/images/manifest.json`
Route/state contract: `../../fixtures/route-contract.json`
Assigned source: `source/`
Assigned evidence: `evidence/`

Begin only after confirming `source/` is empty and every image fixture has `binary_present: true`. Work only inside the assigned condition directory. Do not inspect prior runs, comparisons, another condition directory, coordinator hypotheses, or evaluator materials. Do not import source, CSS, components, scripts, tokens, templates, or generated assets from anywhere except the frozen shared fixtures.

Build the complete runnable experience within the frozen implementation budget. Preserve every required fact, route, state, action, accessibility requirement, responsive requirement, and prohibition. Do not invent content. Do not expose benchmark, condition, validation, or implementation language in the public product.

Produce every required evidence artifact and state the real validation boundary. Do not compare this implementation with any other implementation and do not declare a winner.
