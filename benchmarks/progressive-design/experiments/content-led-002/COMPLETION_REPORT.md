# Setup Completion Record — content-led-002

## Frozen subject

Apollo 11 Learning Paths: a public educational experience in which visitors choose a concise orientation, chronological mission sequence, or people-and-systems learning path.

## Repository state

- Setup branch: `agent/progressive-design-content-led-002-setup`
- Experiment directory: `benchmarks/progressive-design/experiments/content-led-002/`
- Doctrine base commit: `a130906f527ca1d5aa1775628d992c1d3bf0b93e`
- Frozen brief SHA-256: `dc5904b8e3a21eb6b28f476285398e8b27071fd67a0fdebb9442756b6188a1b2`
- Immutable anchor: the full setup commit SHA supplied by the setup pull request and launch message
- Tag dependency: none

A Git commit cannot contain its own SHA without changing that SHA. The setup pull request and the Session 0 completion response therefore record the final full setup commit SHA. Every implementation branch must be created directly from that SHA.

## Freeze confirmation

- Frozen copy and data fixtures are committed.
- Six setup-authored PNG image binaries are committed.
- Image dimensions, alternative text, factual sources, authorship, approval, and SHA-256 values are recorded.
- The route and state contract is committed.
- Accessibility and responsive requirements are committed.
- The equal implementation budget is committed.
- The evidence contract is committed.
- The experiment manifest and initialization receipts are committed.
- Each sealed packet has an adjacent JSON allowlist contract and a minimal launch prompt.
- The packet preflight validator must pass before implementation.

## Blank-state confirmation

Every assigned `source/` directory contains only a zero-byte `.gitkeep`. No site, component, stylesheet, script, layout, visual identity, design token, or reference implementation is present.

## Implementation status

No implementation condition was run or started during setup. No design screenshot was produced. No candidate was compared, ranked, or predicted.
