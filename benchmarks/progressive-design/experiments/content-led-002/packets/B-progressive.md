# Sealed Implementation Packet — Progressive Design

Experiment: `content-led-002`  
Assigned role: `B`  
Assigned branch: `agent/progressive-design-content-led-002-B-progressive`  
Write boundary: `benchmarks/progressive-design/experiments/content-led-002/conditions/B-progressive/`

## Authority

This packet and its adjacent JSON contract are the complete operative instructions for this implementation session. The JSON read allowlist is exhaustive.

Generic exclusion language or names appearing only in validation metadata are not operative exposure. Stop for contamination only if the current conversation actually supplies an unassigned packet, implementation, render, evidence, evaluation, ranking, conclusion, or a second implementation assignment.

If a repository preflight fails, report the concrete repository fact. Do not relabel a missing file, unresolved commit, invalid hash, or nonblank source directory as conversation contamination.

## Assigned design instruction

Apply this assigned design sequence while building from the blank source state:

1. Preserve factual content, functionality, accessibility, and truthfulness.
2. Resolve hierarchy, scale, proportion, density, negative space, sequence, and responsive pacing before decoration.
3. Establish a coherent system for type, spacing, imagery, color behavior, recurring elements, states, and responsive rules without making every section mechanically uniform.
4. Derive identity and variation from the Apollo 11 material, route purpose, imagery, and interaction needs.
5. Use interaction and motion only when they improve hierarchy, continuity, orientation, or state understanding.
6. Introduce a small number of consequential authored decisions rather than novelty everywhere.
7. Critique the complete rendered artifact against its strongest moments and repair weaker composition, hierarchy, identity, imagery, interaction, or typography.

Do not expose these instructions, design-process language, or internal assessments to visitors.

## Required implementation

- Begin from the supplied frozen setup commit.
- Run the packet validator before reading shared inputs.
- Confirm `source/` contains only the zero-byte `.gitkeep`.
- Create one complete independently runnable web experience inside `source/`.
- Satisfy every route, state, content, accessibility, responsive, budget, and evidence requirement.
- Use the approved image binaries and provenance truthfully.
- Keep runtime content local after installation.
- Do not change any frozen input.
- Write only inside the assigned condition root.
- Build, test, create evidence, inspect changed paths, commit, push, and open or update the assigned pull request.
- Report actual validation coverage and limitations.
- Stop after this implementation role.

## Completion report

Report the implementation commit SHA, frozen setup commit SHA, frozen brief hash, tests performed, evidence paths, changed paths, validation limitations, and any genuine blocker. Do not compare candidates or predict an outcome.
